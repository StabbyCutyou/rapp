class <%= class_name %>
  class Env
    def initialize(env)
      @env = env
    end
  
    def to_s
      @env.downcase
    end
  
    def production?
      @env == 'production'
    end
  
    def development?
      @env == 'development'
    end
  end
end